# identity-provider

# Build
On Windows:
```
./build.ps1 -target build
```

On OS X:
```
./build.sh -target build
```

# Run
On Windows:
```
./build.ps1 -target run
```

On OS X:
```
./build.sh -target run
```
